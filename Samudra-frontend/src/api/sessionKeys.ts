/** localStorage key for JWT — must match AuthContext. Not a secret; just the storage slot name. */
export const ACCESS_TOKEN_STORAGE_KEY = 'samudra_access_token';

export const USER_STORAGE_KEY = 'samudra_user';
