# =============================================================================
# Samudra — SANDBOX profile (dev machine + AWS pre-prod blocker deploy)
# Activate: default, or --spring.profiles.active=sandbox
# Mail password: application.properties (gitignored locally) or env SPRING_MAIL_PASSWORD
# =============================================================================

samudra.environment=sandbox

samudra.jwt.secret=sandbox-change-me-use-at-least-32-characters-long-secret
samudra.jwt.expiry-seconds=3600

# Google OAuth — Web client ID (same as frontend GIS). Set GOOGLE_OAUTH_CLIENT_ID env or paste here.
samudra.oauth.google.client-id=633814605101-ca7h7apk44h7qks86gnn9dq3d4sti5el.apps.googleusercontent.com

# Email — Gmail via Google App Password (app: samudra-backend)
samudra.otp.delivery=smtp
samudra.mail.from=samudramarketplace@gmail.com

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=samudramarketplace@gmail.com
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
