# =============================================================================
# Samudra — PRODUCTION profile (AWS deploy — real user email delivery)
# Activate: --spring.profiles.active=prod
# Set SPRING_MAIL_PASSWORD on EC2 / secret manager (same Gmail App Password as sandbox).
# =============================================================================

samudra.environment=prod

samudra.jwt.secret=${SAMUDRA_JWT_SECRET:REPLACE_ME_IN_PROD}
samudra.jwt.expiry-seconds=3600

samudra.oauth.google.client-id=633814605101-ca7h7apk44h7qks86gnn9dq3d4sti5el.apps.googleusercontent.com

samudra.otp.delivery=smtp
samudra.mail.from=${SAMUDRA_MAIL_FROM:samudramarketplace@gmail.com}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/samudra-marketPlace}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:REPLACE_ME_IN_PROD}

# Gmail SMTP — Google App Password (app: samudra-backend)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:samudramarketplace@gmail.com}
spring.mail.password=nszweqwnknqvkkzr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
