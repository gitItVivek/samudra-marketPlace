# =============================================================================
# Samudra — DEPLOY profile (Docker Compose / EC2 Option A)
# Activate: SPRING_PROFILES_ACTIVE=deploy
# All secrets via environment variables (see docker/.env.example)
# =============================================================================

samudra.environment=deploy

samudra.jwt.secret=${SAMUDRA_JWT_SECRET:9vL3fWq7N2xKp8Rz5TmYc4Jh6UdA1BsE0GnQ9XeV7KrM2PtL}
samudra.jwt.expiry-seconds=3600

samudra.oauth.google.client-id=${SAMUDRA_OAUTH_GOOGLE_CLIENT_ID:633814605101-ca7h7apk44h7qks86gnn9dq3d4sti5el.apps.googleusercontent.com}

samudra.otp.delivery=smtp
samudra.mail.from=${SAMUDRA_MAIL_FROM:samudramarketplace@gmail.com}

# CORS — comma-separated patterns. For EC2 IP demo add http://<ip> or use http://* (sandbox only).
samudra.cors.allowed-origin-patterns=${SAMUDRA_CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:*,http://127.0.0.1:*}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/samudra-marketPlace}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:samudramarketplace@gmail.com}
spring.mail.password=nszweqwnknqvkkzr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

samudra.events.kafka.enabled=${SAMUDRA_EVENTS_KAFKA_ENABLED:true}
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
samudra.internal.api.token=${SAMUDRA_INTERNAL_API_TOKEN:samudra-internal-dev}
samudra.marketplace.base-url=${SAMUDRA_MARKETPLACE_BASE_URL:https://samudra.digital}

# Local Docker: V6 was applied earlier with a different file checksum — do not block startup
spring.flyway.enabled=true
spring.flyway.repair-on-migrate=${SPRING_FLYWAY_REPAIR_ON_MIGRATE:true}
spring.flyway.validate-on-migrate=${SPRING_FLYWAY_VALIDATE_ON_MIGRATE:false}

# Redis — interest registration cache + listing search result cache
spring.data.redis.host=${SPRING_REDIS_HOST:redis}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}
samudra.cache.redis.enabled=${SAMUDRA_CACHE_REDIS_ENABLED:true}
samudra.cache.interest-registration-ttl-days=${SAMUDRA_CACHE_INTEREST_TTL_DAYS:90}
samudra.cache.search-results-ttl-minutes=${SAMUDRA_CACHE_SEARCH_TTL_MINUTES:5}
