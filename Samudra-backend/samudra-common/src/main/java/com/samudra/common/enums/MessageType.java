package com.samudra.common.enums;

public enum MessageType {
    TEXT,
    IMAGE,
    OFFER,          // buyer makes a price offer through chat
    SYSTEM          // e.g. "Listing has been sold" system message
}
